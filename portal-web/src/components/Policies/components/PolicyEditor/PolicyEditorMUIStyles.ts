import { makeStyles } from "@material-ui/core/styles"

export const policyEditorOptionStyles = makeStyles({
  root: {
    maxWidth: "600px",
  },
  paper: {
    maxWidth: "600px",
    height: "calc(100% - 175px)",
    top: 175,
  },
})
